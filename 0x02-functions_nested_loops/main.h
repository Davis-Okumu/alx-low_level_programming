#include <stdio.h>
#include <stdlib.h>

/**
 *print_alphabet
 *Void: no return
 */
void print_alphabet()
{
        int ch;
        for(ch = 'a'; ch < 'z'; ch++)
        {
                putchar(ch);
        }
}
