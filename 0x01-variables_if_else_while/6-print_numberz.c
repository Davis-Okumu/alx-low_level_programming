#include <stdio.h>
#include <stdlib.h>

/**
 *main - Entry point
 *Return: always o (success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
