#include <stdio.h>
#include <stdlib.h>

/**
 *main - main enetry
 *Return: always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; n <=9; n++)
	{
		putchar(n);
		putchar(',');
	}
	return (0);
}
