#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to be returned
 * return: a value n
 */
int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
