#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to be returned
 * return: integer,length of a string
 */
int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
